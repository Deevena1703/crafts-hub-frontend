// Mock data removed — all data is now fetched from MongoDB via the Express API.
// See src/lib/api.js for all API calls.
export const categories = [];
export const manufacturers = [];
export const products = [];
